import { DefaultButtonDirective } from './default-button.directive';

describe('DefaultButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultButtonDirective();
    expect(directive).toBeTruthy();
  });
});
